package saveOffset.enums;

public enum S0Enum
{
  BIZTYPE,
  IPS,
  REMOTE_USER,
  TIME,
  URL,
  STATUS,
  BODY_BYTES_SEND,
  REFERER,
  AGENT,
  REMOTE_ADDR,
  REQUEST_TIME,
  UPSTREAM_RESPONSE_TIME
}
